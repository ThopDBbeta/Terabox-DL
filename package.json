{
  "name": "cloudflare-worker-project",
  "version": "1.0.0",
  "description": "Cloudflare Worker deployment",
  "scripts": {
    "dev": "node src/index.ts",
    "deploy": "node src/index.ts",
    "test": "jest"
  },
  "dependencies": {
    "@types/jest": "^29.5.3",
    "esbuild": "^0.18.15",
    "esbuild-jest": "^0.5.0",
    "hono": "^3.2.7",
    "jest": "^29.6.1",
    "jest-environment-miniflare": "^2.14.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20230518.0"
  },
  "engines": {
    "node": ">=18"
  }
}
